
import { createTheme } from '@material-ui/core/styles';

const Theme = createTheme({
  palette: {
    primary: {
      light: '#42b770',
      main: '#00aa58',
      dark: '#009b4e'
    },
    secondary: {
      light: '#d5065a',
      main: '#bf0455',
      dark: '#aa0051'
    }
  },
});

export default Theme;
